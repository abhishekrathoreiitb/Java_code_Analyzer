# Windows-Compatible Requirements for Java RAG Analyzer
# This file uses pre-compiled wheels to avoid build issues

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0
aiofiles==23.2.1

# Use pre-compiled NumPy wheel (no building required)
numpy>=1.21.0,<2.0.0

# Data processing with compatible versions
pandas>=1.5.0,<3.0.0
pydantic>=2.0.0,<3.0.0

# RAG and AI dependencies (simplified for Windows)
chromadb>=0.4.0,<0.5.0
google-generativeai>=0.3.0,<0.4.0

# Sentence transformers with compatible torch
sentence-transformers>=2.2.0,<3.0.0
torch>=1.11.0,<3.0.0 --index-url https://download.pytorch.org/whl/cpu

# Java parsing
javalang==0.13.0

# Utilities
python-dotenv==1.0.0

# Optional: For better performance (install separately if needed)
# scikit-learn>=1.3.0,<2.0.0